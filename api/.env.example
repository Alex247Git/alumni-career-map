# Copy to .env and fill in real values. NEVER commit .env
DB_HOST=127.0.0.1
DB_PORT=3306
DB_NAME=alumni_ds_db
DB_USER=alumni_api
# !! CHANGE THIS !! (used by update_schema.sql GRANT too)
DB_PASS=change-me

# Strong random value, e.g. `openssl rand -base64 32`
JWT_SECRET=change-me-long-random-value
JWT_ISSUER=alumni-career-map