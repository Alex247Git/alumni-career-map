# Convenience pointer — the single canonical stack lives at the repo root.
# Running from api/ still brings up db + api + frontend together.
#
#   cd api && docker compose up -d --build
#   (or from the root: docker compose up -d --build)
include:
  - ../docker-compose.yml
